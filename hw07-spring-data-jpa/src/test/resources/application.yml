spring:
  profiles:
    active: test-flyway
  datasource:
    url: jdbc:h2:mem:testdb
  shell:
    interactive:
      enabled: false
  flyway:
    enabled: true
    locations: classpath:/db/migration
  jpa:
    generate-ddl: false
    hibernate:
      ddl-auto: none
    show-sql: true